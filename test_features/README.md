# test_features/

🔒 **CRITICAL SECURITY WARNING**

This directory is used exclusively for storing local, environment-specific Google Service Account keys [1]. 

* **Do NOT commit the key file:** The file `service-account-key.json` contains highly privileged, live credentials for your **blogerbuzzblogspotcom** Firebase project [1, 2]. It must remain strictly ignored by Git.
* **Setup:** Copy the service account key template from the root configuration and manually populate it locally. Do not upload actual keys to any public branch.
